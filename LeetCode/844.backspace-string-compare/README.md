# LeetCode 844 — Backspace String Compare

A TypeScript practice environment for solving **Backspace String Compare**.

- Read the problem statement in [`PROBLEM.md`](./PROBLEM.md).
- Implement the function in [`src/backspaceCompare.ts`](./src/backspaceCompare.ts).
- Check your solution with `npm test`.

```bash
npm install
npm test
```
