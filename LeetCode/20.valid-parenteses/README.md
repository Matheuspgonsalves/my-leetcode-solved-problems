# LeetCode 20 — Valid Parentheses

A TypeScript practice environment for solving **Valid Parentheses**.

- Read the problem statement in [`PROBLEM.md`](./PROBLEM.md).
- Implement the function in [`src/isValid.ts`](./src/isValid.ts).
- Check your solution with `npm test`.

```bash
npm install
npm test
```
